# This file is kept only for backwards compatibility with older deploys.
# New deployments should use `portfolio_project/.env.example` as a template
# and put real values into a project-root `.env` file (gitignored).
#
# DATABASE_URL=postgres://user:password@host:5432/dbname
# REDIS_URL=redis://localhost:6379
# ENCRYPTION_KEY=change-me
